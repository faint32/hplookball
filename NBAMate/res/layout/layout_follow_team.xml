<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <RelativeLayout
        android:id="@+id/layout_title_bar"
        style="@style/bg_title_bar" >
<!--  -->
        <Button
            android:id="@+id/btn_setup"
           style="@style/btn_back"/>

        <TextView
            android:id="@+id/txt_title"
            style="@style/txt_title"
            android:text="@string/title_my_follow" />
    </RelativeLayout>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@color/follow_team_bg"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/txt_team"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:text="@string/str_follow_team"
            android:textColor="@color/res_cor4"
            android:textSize="15sp" >
        </TextView>

        <ExpandableListView
            android:id="@+id/list_team"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:cacheColorHint="#00000000"
            android:divider="#00000000"
            android:dividerHeight="0dp"
            android:drawSelectorOnTop="false"
            android:childDivider="@color/transform"
            android:listSelector="#00000000"
            android:scrollingCache="false" />
    </LinearLayout>

</LinearLayout>